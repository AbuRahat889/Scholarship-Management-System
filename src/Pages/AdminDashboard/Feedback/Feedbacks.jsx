import Feedback from "../../../Components/Feedback/Feedback";

const Feedbacks = ({ modal }) => {
  return (
    <div>
      <Feedback modal={modal}></Feedback>
    </div>
  );
};

export default Feedbacks;
