// Applicant phone number,
// Applicant photo,
// Applicant address (village,district,country),
// Applicant gender ( by dropdown ) ,
// Applicant Applying Degree( by dropdown total three degree:
// Diploma,Bachelor,Masters),
// SSC Result,
// HSC result,
// Study gap ( optional if have user select by the dropdown)
// University name:(read only)
// Scholarship category:(read only)
// Subject Category(read only),

// User name,
// User _id(if you think you will need in future),
// User Email,
// Scholarship _id,
// Current date

const ApplyScholarship = () => {
    return (
        <div>
            
        </div>
    );
};

export default ApplyScholarship;